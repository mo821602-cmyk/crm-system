# JWT Configuration
JWT_SECRET=your-strong-secret-key-here-change-in-production

# Server Configuration
PORT=3000
NODE_ENV=development

# Database (SQLite is embedded, no additional config needed)
# Database file will be created automatically in data/crm.db
